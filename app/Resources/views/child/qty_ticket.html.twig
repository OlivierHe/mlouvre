{# app/Resources/views/child/qty_ticket.html.twig #}
{% extends 'base.html.twig' %}

{% block breadcrumb %}
 <div class="btn-group btn-breadcrumb">
    <a href="/{{ app.request.locale }}" class="btn btn-default">
    <i class="glyphicon glyphicon-home">&nbsp;{{ "Accueil" | trans}}</i></a>
    <a href="/quantite_ticket" class="btn btn-default">{{ "Quantité de billets" | trans }}</a>
</div>
{% endblock %}

{% block section %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary marge-btn" value="{{ "Continuer la réservation" | trans}}"/>
    {{ form_end(form) }}
{% endblock %}